DROP TABLE PRICES IF EXISTS;

CREATE TABLE PRICES  (
    BRAND_ID BIGINT, --IDENTITY NOT NULL PRIMARY KEY,
    START_DATE DATE,
    END_DATE DATE,
    PRICE_LIST BIGINT,
    PRODUCT_ID BIGINT,
    PRICE NUMBER(5,2),
    CURR VARCHAR(3)
);